using Microsoft.AspNetCore.Identity;

namespace TradeWeb.API.Data
{
    /// <summary>
    /// TODO : Add profile data for application users by adding properties to the AppIdentityUser class (For Identity User)
    /// </summary>
    public class AppUser : IdentityUser
    {
    }
}
